# Postgres (matches docker-compose db service)
DATABASE_URL="postgresql://miraj-cv:miraj-cv@localhost:5433/miraj-cv"

# Docx rendering microservice
DOCX_SERVICE_URL="http://localhost:8001"

# Claude API (set for real, never commit this)
ANTHROPIC_API_KEY=""

# Auth (Phase 0 stub — swap in real Clerk/Auth.js keys when wired up)
# AUTH_SECRET=""
